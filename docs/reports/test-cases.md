# Test Case Inventory

Generated from the current automated test suite in `tests/`.

- Last verified: 2026-05-12
- Verification command: `uv run pytest`
- Result: 130 passed
- Status legend: `Passed` means the automated test passed in the latest verification run.

| ID Test Case | Feature | Test case name | Objective | Step-by-step | Data input | Expected result | Status |
|---|---|---|---|---|---|---|---|
| TC-001 | Access Policy | `test_access_policy_allows_named_shell_actions_only` | Verify only named shell access actions are authorized. | 1. Arrange allowed and disallowed shell actions.<br>2. Evaluate the access policy.<br>3. Assert allowed actions pass and unrelated actions fail. | Shell action names. | Only explicitly named shell actions are allowed. | Passed |
| TC-002 | Access Policy | `test_access_policy_allows_named_admin_management_actions_only` | Verify admin management access is limited to named actions. | 1. Arrange admin action names.<br>2. Evaluate the policy.<br>3. Assert only supported admin actions are allowed. | Admin management action names. | Only named admin management actions are allowed. | Passed |
| TC-003 | Approval Letter Workflow | `test_student_accesses_stable_generated_approval_letter_metadata` | Ensure generated approval letter metadata is stable for repeated student access. | 1. Create facility, organization unit, student, and reservation.<br>2. Request approval letter metadata twice.<br>3. Compare returned metadata. | Student account, active facility, reservation, fixed clock. | Both requests return identical PDF metadata for the same reservation. | Passed |
| TC-004 | Approval Letter Workflow | `test_student_downloads_generated_approval_letter_pdf_multiple_times` | Ensure students can download the same generated approval letter PDF repeatedly. | 1. Create a reservation.<br>2. Download the approval letter twice.<br>3. Compare PDF response headers and content. | Reservation code, student profile, facility details. | Both downloads return the same PDF attachment with official reservation content. | Passed |
| TC-005 | Approval Letter Workflow | `test_generated_approval_letter_pdf_includes_official_reservation_details` | Verify the approval letter PDF contains official reservation details. | 1. Create student reservation data.<br>2. Download the generated approval letter.<br>3. Inspect PDF bytes for required fields. | Reservation date, time, NIM, phone, organization, participant count. | PDF includes generated date, schedule, requester, organization, participant, signature, and contact details. | Passed |
| TC-006 | Approval Letter Workflow | `test_student_cannot_access_another_students_approval_letter` | Prevent students from accessing another student's approval letter. | 1. Create reservation owned by one student.<br>2. Authenticate as another student.<br>3. Request the owner's approval letter. | Two student accounts and one owned reservation. | Non-owner access is rejected. | Passed |
| TC-007 | Approval Letter Workflow | `test_student_uploads_signed_approval_letter_after_generated_letter_exists` | Allow signed approval letter upload only after generation. | 1. Generate approval letter for a reservation.<br>2. Upload signed PDF as the owner.<br>3. Retrieve reservation status. | Signed PDF file, owner token, reservation ID. | Upload succeeds and reservation moves into document review. | Passed |
| TC-008 | Approval Letter Workflow | `test_student_cannot_upload_signed_approval_letter_before_generated_letter_exists` | Reject signed letter uploads before the approval letter is generated. | 1. Create a reservation.<br>2. Upload a signed PDF before generating the letter.<br>3. Assert error response. | Signed PDF file, reservation without generated letter. | Upload is rejected and reservation state is unchanged. | Passed |
| TC-009 | Approval Letter Workflow | `test_student_upload_signed_approval_letter_rejects_unsupported_file_type` | Validate signed letter file type restrictions. | 1. Generate approval letter.<br>2. Upload unsupported file type.<br>3. Assert validation error. | Non-PDF signed letter file. | API rejects unsupported file type. | Passed |
| TC-010 | Approval Letter Workflow | `test_student_upload_signed_approval_letter_rejects_files_larger_than_five_mb` | Validate signed letter size limit. | 1. Generate approval letter.<br>2. Upload PDF larger than 5 MB.<br>3. Assert validation error. | Oversized PDF file. | API rejects files larger than 5 MB. | Passed |
| TC-011 | Approval Letter Workflow | `test_assigned_staff_approval_moves_free_facility_reservation_to_approved` | Ensure assigned staff can approve signed documents for free facilities. | 1. Assign staff to facility.<br>2. Move reservation to document review.<br>3. Approve as assigned staff. | Free facility, assigned staff, signed approval letter. | Reservation becomes approved without payment flow. | Passed |
| TC-012 | Approval Letter Workflow | `test_assigned_staff_downloads_uploaded_signed_approval_letter_for_review` | Ensure assigned staff can download uploaded signed letters for review. | 1. Upload signed approval letter.<br>2. Authenticate assigned staff.<br>3. Download the uploaded file. | Assigned staff token, uploaded PDF. | Download succeeds with original signed letter content. | Passed |
| TC-013 | Approval Letter Workflow | `test_unassigned_staff_cannot_approve_signed_approval_letter` | Prevent unassigned staff from approving a reservation document. | 1. Create assigned and unassigned staff.<br>2. Move reservation to document review.<br>3. Try approval as unassigned staff. | Reservation under another staff assignment. | Approval is denied. | Passed |
| TC-014 | Approval Letter Workflow | `test_assigned_staff_approval_moves_paid_facility_reservation_to_pending_payment` | Route paid reservations to payment after document approval. | 1. Create paid facility reservation.<br>2. Upload signed approval letter.<br>3. Approve document as assigned staff. | Paid facility price and payment instructions. | Reservation moves to pending payment with upload deadline. | Passed |
| TC-015 | Approval Letter Workflow | `test_assigned_staff_rejection_requires_reason` | Require a reason when staff reject approval documents. | 1. Move reservation to document review.<br>2. Submit rejection without reason.<br>3. Assert validation error. | Empty rejection reason. | Rejection is rejected by validation. | Passed |
| TC-016 | Approval Letter Workflow | `test_assigned_staff_rejection_rejects_reservation_without_revision` | Verify document rejection finalizes reservation as rejected. | 1. Move reservation to document review.<br>2. Reject with a reason.<br>3. Read reservation status and reason. | Rejection reason. | Reservation is rejected and reason is recorded. | Passed |
| TC-017 | Assigned Facility Access | `test_assigned_facility_access_returns_facility_for_assigned_staff` | Confirm staff can access assigned facility records. | 1. Create facility and staff assignment.<br>2. Request assigned facility access.<br>3. Assert returned facility. | Assigned staff ID, facility ID. | Assigned facility is returned. | Passed |
| TC-018 | Assigned Facility Access | `test_assigned_facility_access_denies_unassigned_staff` | Deny facility access to unassigned staff. | 1. Create facility without assignment for staff.<br>2. Request access.<br>3. Assert denial. | Unassigned staff ID, facility ID. | Access is denied. | Passed |
| TC-019 | Assigned Facility Access | `test_assigned_facility_access_reports_missing_facility` | Report missing facilities through assigned access checks. | 1. Request a nonexistent facility.<br>2. Evaluate access.<br>3. Assert missing result. | Missing facility ID. | Missing facility is reported distinctly. | Passed |
| TC-020 | Authentication Foundation | `test_user_account_module_uses_named_institutional_email_policy` | Verify institutional email domain policy normalization. | 1. Configure allowed domains.<br>2. Check allowed and disallowed emails.<br>3. Assert policy decisions. | `apps.ipb.ac.id`, `ipb.ac.id`, Gmail address. | IPB domains are accepted; non-institutional email is rejected. | Passed |
| TC-021 | Authentication Foundation | `test_user_account_module_returns_public_user_account_not_persistence_record` | Ensure registration returns a public account without password hash. | 1. Register a student through account module.<br>2. Inspect returned account.<br>3. Inspect persisted user separately. | Student registration data and password. | Public account omits password hash while persistence stores hashed password. | Passed |
| TC-022 | Authentication Foundation | `test_user_account_module_issues_sessions_through_session_codec` | Ensure login issues sessions through the session codec. | 1. Register a student.<br>2. Login with valid credentials.<br>3. Assert issued access token. | Email and password. | Login returns session token from configured codec. | Passed |
| TC-023 | Authentication Foundation | `test_student_registers_with_allowed_email_and_reaches_student_shell` | Verify student registration, login, and student shell access. | 1. Try invalid email registration.<br>2. Register with IPB email and login.<br>3. Access student shell. | Gmail email, IPB email, password, NIM, phone. | Invalid email is rejected; valid student reaches shell. | Passed |
| TC-024 | Authentication Foundation | `test_active_authenticated_user_can_get_current_user_identity` | Allow active authenticated users to read current identity. | 1. Register and login a student.<br>2. Call `/auth/me` with bearer token.<br>3. Assert identity payload. | Valid bearer token. | Current user profile is returned. | Passed |
| TC-025 | Authentication Foundation | `test_current_user_identity_requires_bearer_credentials` | Require bearer credentials for current identity. | 1. Call `/auth/me` without credentials.<br>2. Assert response.<br>3. Verify error detail. | No authorization header. | Request returns authentication required error. | Passed |
| TC-026 | Authentication Foundation | `test_current_user_identity_rejects_invalid_tokens` | Reject invalid bearer tokens. | 1. Call `/auth/me` with invalid token.<br>2. Assert response status.<br>3. Verify error detail. | `Bearer not-a-valid-token`. | Request returns invalid token error. | Passed |
| TC-027 | Authentication Foundation | `test_current_user_identity_rejects_inactive_user_tokens` | Reject tokens for inactive users. | 1. Create inactive user and token.<br>2. Call `/auth/me`.<br>3. Assert error. | Inactive student token. | Request returns inactive account error. | Passed |
| TC-028 | Authentication Foundation | `test_super_admin_creates_staff_and_role_shells_are_guarded` | Verify super admin staff creation and role-based shell access. | 1. Login as super admin.<br>2. Create staff user.<br>3. Login as staff and access role shells. | Super admin account, staff creation payload. | Staff is created and role shells enforce correct roles. | Passed |
| TC-029 | Authentication Foundation | `test_inactive_users_cannot_login_or_refresh_sessions` | Ensure inactive users cannot login or refresh sessions. | 1. Create inactive user.<br>2. Attempt login and token refresh.<br>3. Assert failures. | Inactive user credentials and token. | Login and refresh are rejected. | Passed |
| TC-030 | Backend Package Layout | `test_application_builds_from_refined_plan_package_layout` | Verify application package layout can build the app. | 1. Import app package structure.<br>2. Create the application.<br>3. Assert expected routes/services are wired. | Application package modules. | App builds successfully from the refined package layout. | Passed |
| TC-031 | Booking Settings | `test_booking_settings_module_uses_repository_seam_for_stored_values` | Ensure booking settings are read through repository abstraction. | 1. Provide stored booking setting values.<br>2. Load settings through module.<br>3. Assert domain settings. | Repository-backed setting rows. | Module returns settings from repository seam. | Passed |
| TC-032 | Booking Settings | `test_super_admin_views_default_booking_settings` | Allow super admin to view default booking settings. | 1. Login as super admin.<br>2. Request booking settings.<br>3. Assert default values. | Super admin token. | Default booking settings are returned. | Passed |
| TC-033 | Booking Settings | `test_super_admin_updates_booking_settings_used_by_student_registration` | Verify updated booking settings affect reservation time validation. | 1. Update settings as super admin.<br>2. Register/login student.<br>3. Validate reservation time using new limits. | Minimum lead time and maximum advance window values. | New settings are persisted and used by reservation validation. | Passed |
| TC-034 | Booking Settings | `test_invalid_booking_settings_update_returns_stable_error_without_partial_changes` | Reject invalid booking settings atomically. | 1. Submit invalid settings update.<br>2. Read settings afterward.<br>3. Assert no partial update. | Invalid booking settings payload. | Stable validation error is returned and previous settings remain. | Passed |
| TC-035 | Booking Settings | `test_booking_settings_schema_converts_to_and_from_domain_settings` | Validate schema and domain settings conversion. | 1. Build domain settings.<br>2. Convert to response schema and back.<br>3. Assert equivalent values. | Domain booking setting values. | Conversion preserves settings values. | Passed |
| TC-036 | Booking Settings | `test_booking_settings_ignore_unrelated_system_setting_rows` | Ignore unrelated system settings when loading booking settings. | 1. Store booking and unrelated settings.<br>2. Load booking settings.<br>3. Assert only booking keys matter. | Mixed system setting rows. | Unrelated rows do not affect booking settings. | Passed |
| TC-037 | Cancellation Workflow | `test_student_can_cancel_own_pre_approval_reservation_but_not_another_students` | Allow owner cancellation before approval and deny other students. | 1. Create reservation for one student.<br>2. Attempt cancellation as owner and another student.<br>3. Assert outcomes. | Two students, pre-approval reservation. | Owner can cancel; non-owner is denied. | Passed |
| TC-038 | Cancellation Workflow | `test_approved_paid_reservation_requires_cancellation_request_reason_and_returns_refund_warning` | Require cancellation request reason for approved paid reservations. | 1. Create approved paid reservation.<br>2. Submit cancellation request with and without reason.<br>3. Assert warning and validation. | Paid approved reservation, cancellation reason. | Reason is required and refund warning is returned. | Passed |
| TC-039 | Cancellation Workflow | `test_assigned_staff_reviews_cancellation_request_while_slot_stays_blocked` | Ensure assigned staff reviews cancellation while slot remains blocked. | 1. Submit cancellation request.<br>2. Review as assigned staff.<br>3. Check calendar blocking behavior. | Assigned staff, cancellation request. | Review decision is recorded and slot remains blocked until resolved. | Passed |
| TC-040 | Deadline Worker | `test_approved_past_reservation_is_effectively_completed_before_worker_persistence` | Treat past approved reservations as completed before persistence update. | 1. Create approved reservation in the past.<br>2. Read effective status.<br>3. Assert completed status. | Approved reservation ending before current time. | Effective status is completed. | Passed |
| TC-041 | Deadline Worker | `test_deadline_worker_expires_missed_student_upload_deadlines_and_marks_staff_overdue` | Process missed student deadlines and staff verification overdue states. | 1. Create reservations with missed upload and verification deadlines.<br>2. Run worker.<br>3. Assert status changes. | Expired document/payment upload and staff verification deadlines. | Student delays expire; staff overdue states are marked. | Passed |
| TC-042 | Deadline Worker | `test_deadline_worker_enforces_normal_and_staff_overdue_final_cutoffs` | Enforce normal and staff-overdue final cutoffs. | 1. Create reservations near deadline cutoffs.<br>2. Run deadline processing.<br>3. Assert final statuses. | Normal cutoff and staff-overdue cutoff timestamps. | Reservations past final cutoffs transition correctly. | Passed |
| TC-043 | Deadline Worker | `test_staff_missed_verification_gets_overdue_cutoff_instead_of_normal_cutoff` | Apply overdue cutoff when staff miss verification. | 1. Create reservation awaiting staff verification.<br>2. Move clock past verification due time.<br>3. Run worker. | Staff verification due timestamp. | Reservation receives staff-overdue cutoff instead of normal cutoff. | Passed |
| TC-044 | Deadline Worker | `test_deadline_worker_persists_completed_status_after_approved_reservation_end_time` | Persist completed status after approved reservation end time. | 1. Create approved reservation ending in the past.<br>2. Run worker.<br>3. Reload reservation. | Approved past reservation. | Completed status is persisted. | Passed |
| TC-045 | Deployment Readiness | `test_railway_backend_deployment_runs_fastapi_and_smoke_checks_health` | Verify Railway backend deployment command and health smoke check. | 1. Read deployment configuration.<br>2. Check FastAPI startup command.<br>3. Assert health smoke check route is covered. | `railway.toml` and backend health configuration. | Deployment config runs FastAPI and supports smoke checks. | Passed |
| TC-046 | Deployment Readiness | `test_backend_deployment_guide_covers_demo_readiness_topics` | Ensure deployment guide covers demo readiness. | 1. Read backend deployment guide.<br>2. Check required topics.<br>3. Assert coverage. | Deployment documentation. | Guide includes expected demo readiness topics. | Passed |
| TC-047 | Deployment Readiness | `test_backend_dependencies_include_postgresql_runtime_driver` | Ensure PostgreSQL driver exists for runtime deployment. | 1. Read dependency configuration.<br>2. Locate PostgreSQL driver dependency.<br>3. Assert presence. | Project dependencies. | PostgreSQL runtime driver is included. | Passed |
| TC-048 | Development Seed | `test_dev_seed_creates_fixed_demo_accounts_with_login_credentials` | Verify dev seed creates fixed demo accounts. | 1. Run dev seed against temporary database.<br>2. Query seeded users.<br>3. Assert demo credentials exist. | Temporary SQLite database. | Fixed demo accounts are created with login credentials. | Passed |
| TC-049 | Development Seed | `test_dev_seed_refuses_production_environment` | Prevent dev seed in explicit production environment. | 1. Run seed with production environment value.<br>2. Capture result.<br>3. Assert refusal. | Production environment setting. | Seed refuses to run. | Passed |
| TC-050 | Development Seed | `test_dev_seed_refuses_production_environment_from_process_env` | Prevent dev seed when process environment is production. | 1. Set process environment to production.<br>2. Run seed.<br>3. Assert refusal. | Production process environment variable. | Seed refuses to run. | Passed |
| TC-051 | Development Seed | `test_dev_seed_creates_frontend_reservation_tracer_data` | Seed demo reservation tracer data for frontend flows. | 1. Run dev seed.<br>2. Query reservation-related demo data.<br>3. Assert tracer records exist. | Temporary database and seed fixture data. | Frontend reservation tracer data is created. | Passed |
| TC-052 | Development Seed | `test_dev_seed_is_idempotent_for_seeded_database_rows` | Ensure dev seed can run repeatedly without duplicates. | 1. Run dev seed twice.<br>2. Count seeded rows.<br>3. Assert stable row counts. | Temporary database. | Repeated seed runs do not duplicate seeded rows. | Passed |
| TC-053 | Facility Browsing | `test_facility_catalog_module_projects_public_catalog_items_through_repository_seam` | Verify catalog module projects public facility summaries. | 1. Provide facility catalog records through reader.<br>2. List active facilities.<br>3. Assert public projection. | Facility record with image and price. | Public catalog item includes cover image and price summary. | Passed |
| TC-054 | Facility Browsing | `test_facility_availability_module_concentrates_reservation_time_rules` | Centralize reservation availability time rules. | 1. Provide availability facts.<br>2. Check facility availability.<br>3. Assert unavailable reason. | Open hour and overlapping reservation facts. | Availability is false with reserved-time reason. | Passed |
| TC-055 | Facility Browsing | `test_students_browse_active_facilities_from_catalog` | List only active facilities in public catalog. | 1. Create active and inactive facilities.<br>2. Request `/facilities`.<br>3. Assert returned list. | Active and inactive facility records. | Only active facility appears in catalog. | Passed |
| TC-056 | Facility Browsing | `test_students_view_facility_detail_public_information` | Show public facility detail information. | 1. Create facility with images.<br>2. Request facility detail.<br>3. Assert public fields. | Facility name, location, capacity, contact, images. | Detail response includes public information, images, price, and reviews summary. | Passed |
| TC-057 | Facility Browsing | `test_paid_facility_detail_shows_price_status_and_summary` | Display paid facility price status and formatted summary. | 1. Create paid facility.<br>2. Request facility detail.<br>3. Assert price section. | Facility price `250000`. | Price is shown as paid with `Rp250.000` summary. | Passed |
| TC-058 | Facility Browsing | `test_students_view_public_facility_calendar_without_private_reservation_data` | Expose public calendar without private reservation data. | 1. Create facility reservations.<br>2. Request public calendar range.<br>3. Assert sanitized entries. | Facility ID, calendar start/end, reservations. | Calendar shows blocked times without private requester details. | Passed |
| TC-059 | Facility Browsing | `test_students_check_facility_availability_against_open_hours` | Check availability against facility open hours. | 1. Create facility open hours.<br>2. Request availability inside and outside hours.<br>3. Assert reasons. | Facility schedule and requested time range. | Outside open hours is unavailable; valid open time can be available. | Passed |
| TC-060 | Facility Browsing | `test_students_check_facility_availability_against_blackout_periods` | Check availability against blackout periods. | 1. Create blackout period.<br>2. Request overlapping availability.<br>3. Assert unavailable result. | Facility blackout time range. | Overlapping blackout makes facility unavailable. | Passed |
| TC-061 | Facility Browsing | `test_students_check_facility_availability_against_blocking_reservations` | Check availability against blocking reservations. | 1. Create existing blocking reservation.<br>2. Request overlapping availability.<br>3. Assert unavailable result. | Existing reservation and requested overlap. | Overlapping blocking reservation makes facility unavailable. | Passed |
| TC-062 | Facility Browsing | `test_students_validate_available_reservation_time_selection` | Validate an available reservation time selection. | 1. Create open facility schedule.<br>2. Submit valid time selection.<br>3. Assert available response. | Valid start and end timestamps. | Time selection is accepted as available. | Passed |
| TC-063 | Facility Browsing | `test_reservation_time_selection_accepts_jakarta_local_timestamps_for_open_hours` | Accept Jakarta local timestamps for open-hour validation. | 1. Create Jakarta open hours.<br>2. Submit local timestamp range.<br>3. Assert acceptance. | Jakarta local start/end timestamps. | Local timestamps are interpreted correctly and accepted. | Passed |
| TC-064 | Facility Browsing | `test_reservation_time_selection_rejects_times_outside_five_minute_increments` | Reject times not aligned to five-minute increments. | 1. Submit range with non-5-minute boundary.<br>2. Validate selection.<br>3. Assert error. | Start/end timestamp with invalid minute increment. | Selection is rejected. | Passed |
| TC-065 | Facility Browsing | `test_reservation_time_selection_rejects_duration_below_one_hour` | Reject reservation duration below one hour. | 1. Submit short time range.<br>2. Validate selection.<br>3. Assert error. | Start/end less than one hour apart. | Selection is rejected. | Passed |
| TC-066 | Facility Browsing | `test_reservation_time_selection_rejects_ranges_crossing_midnight_in_jakarta` | Reject time ranges crossing midnight in Jakarta time. | 1. Submit range crossing local midnight.<br>2. Validate selection.<br>3. Assert error. | Jakarta local timestamps crossing midnight. | Selection is rejected. | Passed |
| TC-067 | Facility Browsing | `test_reservation_time_selection_rejects_times_outside_facility_open_hours` | Reject reservation times outside facility open hours. | 1. Create open hours.<br>2. Submit outside-hours range.<br>3. Assert rejection. | Outside-hours start/end timestamps. | Selection is rejected with open-hours reason. | Passed |
| TC-068 | Facility Browsing | `test_reservation_time_selection_rejects_blackout_periods` | Reject time selection overlapping blackouts. | 1. Create blackout period.<br>2. Submit overlapping range.<br>3. Assert rejection. | Requested time overlapping blackout. | Selection is rejected. | Passed |
| TC-069 | Facility Browsing | `test_reservation_time_selection_rejects_blocking_reservations` | Reject time selection overlapping blocking reservations. | 1. Create blocking reservation.<br>2. Submit overlapping range.<br>3. Assert rejection. | Requested time overlapping existing reservation. | Selection is rejected. | Passed |
| TC-070 | Facility Browsing | `test_reservation_time_selection_rejects_start_less_than_default_lead_time` | Enforce default minimum lead time. | 1. Set current clock.<br>2. Submit start time too soon.<br>3. Assert rejection. | Start time before default lead-time limit. | Selection is rejected. | Passed |
| TC-071 | Facility Browsing | `test_reservation_time_selection_rejects_start_beyond_default_advance_window` | Enforce default maximum advance window. | 1. Set current clock.<br>2. Submit start time too far ahead.<br>3. Assert rejection. | Start time beyond default advance window. | Selection is rejected. | Passed |
| TC-072 | Facility Browsing | `test_reservation_time_selection_uses_configured_minimum_lead_time` | Use configured minimum lead time for validation. | 1. Configure custom lead time.<br>2. Submit boundary time ranges.<br>3. Assert configured behavior. | Custom minimum lead-time setting. | Validation follows configured lead time. | Passed |
| TC-073 | Facility Browsing | `test_reservation_time_selection_uses_configured_maximum_advance_window` | Use configured maximum advance window for validation. | 1. Configure custom advance window.<br>2. Submit boundary time ranges.<br>3. Assert configured behavior. | Custom maximum advance-window setting. | Validation follows configured advance window. | Passed |
| TC-074 | HTTP Application | `test_http_runtime_module_translates_missing_credentials_to_http_error` | Translate missing credentials into HTTP errors. | 1. Invoke protected runtime dependency without credentials.<br>2. Capture HTTP error.<br>3. Assert response detail. | Missing credentials. | Runtime returns authentication HTTP error. | Passed |
| TC-075 | HTTP Application | `test_http_application_module_uses_runtime_dependency_registry_for_route_wiring` | Ensure routes use runtime dependency registry. | 1. Inspect application route wiring.<br>2. Resolve dependencies.<br>3. Assert registry usage. | App dependency registry. | Routes are wired through runtime dependency registry. | Passed |
| TC-076 | HTTP Application | `test_http_application_module_builds_app_with_foundation_routes` | Verify app builds with foundation routes. | 1. Create application.<br>2. Inspect routes.<br>3. Assert foundation endpoints exist. | Application factory. | App includes foundation routes. | Passed |
| TC-077 | Notifications | `test_student_views_reservation_notification_inbox_and_marks_item_read` | Allow students to view notifications and mark one read. | 1. Create reservation notification.<br>2. Request inbox as student.<br>3. Mark item read and re-read. | Student token and notification item. | Inbox returns notification and read status updates. | Passed |
| TC-078 | Notifications | `test_signed_letter_upload_notifies_assigned_staff_and_super_admin_only` | Notify assigned staff and super admin after signed letter upload. | 1. Upload signed approval letter.<br>2. Read notification inboxes.<br>3. Assert recipients. | Assigned staff, unassigned staff, super admin, student upload. | Only assigned staff and super admin receive notification. | Passed |
| TC-079 | Notifications | `test_overdue_verification_notification_includes_facility_tu_contact` | Include TU contact in overdue verification notifications. | 1. Create overdue verification case.<br>2. Run deadline processing.<br>3. Read notification content. | Facility TU contact and overdue reservation. | Notification includes facility TU contact information. | Passed |
| TC-080 | Organization Units | `test_organization_unit_management_returns_public_profile_not_persistence_record` | Return public organization unit profile, not persistence record. | 1. Create organization unit through module.<br>2. Inspect returned object.<br>3. Assert public fields. | Organization unit profile fields. | Public profile is returned without persistence-only details. | Passed |
| TC-081 | Organization Units | `test_organization_unit_management_translates_duplicate_names` | Translate duplicate organization unit names into domain error. | 1. Create organization unit.<br>2. Try same name again.<br>3. Assert duplicate-name error. | Duplicate organization unit name. | Duplicate is rejected with stable domain error. | Passed |
| TC-082 | Organization Units | `test_super_admin_creates_organization_unit` | Allow super admin to create organization units. | 1. Login as super admin.<br>2. Submit organization unit payload.<br>3. Assert created response. | Name, description/profile fields. | Organization unit is created. | Passed |
| TC-083 | Organization Units | `test_super_admin_cannot_create_duplicate_organization_unit_name` | Prevent duplicate organization unit creation. | 1. Create organization unit.<br>2. Submit duplicate create request.<br>3. Assert validation error. | Duplicate organization unit name. | Duplicate create request is rejected. | Passed |
| TC-084 | Organization Units | `test_super_admin_updates_organization_unit_profile_fields` | Allow super admin to update organization unit profile fields. | 1. Create organization unit.<br>2. Submit update as super admin.<br>3. Read updated unit. | Updated profile fields. | Organization unit fields are updated. | Passed |
| TC-085 | Organization Units | `test_super_admin_deactivates_and_reactivates_organization_unit` | Allow super admin to deactivate and reactivate organization units. | 1. Create organization unit.<br>2. Deactivate it.<br>3. Reactivate it and assert state. | Organization unit ID and active flags. | Active status toggles correctly. | Passed |
| TC-086 | Organization Units | `test_students_list_only_active_organization_units_for_selection` | Show only active organization units to students. | 1. Create active and inactive units.<br>2. Request student selection list.<br>3. Assert returned units. | Active and inactive organization units. | Only active units are listed. | Passed |
| TC-087 | Payment Workflow | `test_student_sees_payment_instructions_only_after_paid_reservation_reaches_pending_payment` | Show payment instructions only after paid reservation reaches pending payment. | 1. Create paid reservation.<br>2. Try payment view before document approval.<br>3. Approve document and view payment. | Paid facility, payment instructions, reservation. | Early access returns conflict; pending payment returns instructions. | Passed |
| TC-088 | Payment Workflow | `test_student_uploads_payment_receipt_for_pending_paid_reservation` | Allow student to upload payment receipt for pending paid reservation. | 1. Move paid reservation to pending payment.<br>2. Upload image receipt.<br>3. Read reservation. | JPG receipt file. | Upload succeeds and verification deadline is recorded. | Passed |
| TC-089 | Payment Workflow | `test_student_payment_receipt_upload_rejects_invalid_files_without_changing_status` | Reject invalid payment receipt files without status changes. | 1. Move paid reservation to pending payment.<br>2. Upload unsupported and oversized files.<br>3. Read reservation status. | PDF receipt and oversized PNG. | Both uploads are rejected and status remains pending payment. | Passed |
| TC-090 | Payment Workflow | `test_assigned_staff_downloads_and_approves_payment_receipt` | Allow assigned staff to download and approve payment receipt. | 1. Upload receipt for paid reservation.<br>2. Download as assigned staff.<br>3. Approve payment. | Assigned staff, receipt image, pending payment reservation. | Receipt downloads and reservation becomes approved. | Passed |
| TC-091 | Payment Workflow | `test_assigned_staff_payment_rejection_requires_reason_and_rejects_reservation` | Require reason for payment rejection and reject reservation when supplied. | 1. Upload receipt.<br>2. Reject without reason, then with reason.<br>3. Assert status and reason. | Rejection reason. | Empty reason is rejected; valid rejection rejects reservation. | Passed |
| TC-092 | Payment Workflow | `test_payment_workflow_blocks_free_reservations_other_students_and_unassigned_staff` | Block payment workflow access for invalid actors and free reservations. | 1. Create free and paid reservations.<br>2. Try payment actions as wrong student and unassigned staff.<br>3. Assert failures. | Free reservation, other student token, unassigned staff token. | Unauthorized or inapplicable payment actions are blocked. | Passed |
| TC-093 | Public Facility Calendar | `test_public_facility_calendar_exposes_holding_and_approved_reservation_statuses` | Expose holding and approved reservation blocks on public calendar. | 1. Create reservations in holding and approved statuses.<br>2. Query public calendar.<br>3. Assert included statuses. | Holding and approved reservations. | Calendar exposes blocking reservation statuses. | Passed |
| TC-094 | Public Facility Calendar | `test_public_facility_calendar_hides_terminal_reservation_statuses` | Hide terminal reservation statuses from public calendar. | 1. Create terminal-status reservations.<br>2. Query public calendar.<br>3. Assert omitted entries. | Cancelled/rejected/completed reservations. | Terminal reservations are hidden from calendar. | Passed |
| TC-095 | Public Facility Reviews | `test_public_facility_reviews_exclude_deleted_reviews_and_summarize_visible_ratings` | Exclude deleted reviews and summarize visible ratings. | 1. Create visible and deleted reviews.<br>2. Request facility public reviews.<br>3. Assert list and summary. | Review ratings and deleted flag. | Deleted reviews are excluded and summary uses visible reviews only. | Passed |
| TC-096 | Reservation Lifecycle | `test_document_approval_moves_free_reservation_to_approved_without_payment_deadline` | Free reservation approval skips payment deadline. | 1. Create free reservation in document review.<br>2. Approve document.<br>3. Inspect reservation state. | Free reservation. | Status becomes approved and no payment deadline is set. | Passed |
| TC-097 | Reservation Lifecycle | `test_document_approval_moves_paid_reservation_to_payment_with_upload_deadline` | Paid reservation approval creates payment upload deadline. | 1. Create paid reservation in document review.<br>2. Approve document.<br>3. Inspect reservation state. | Paid reservation. | Status becomes pending payment with upload deadline. | Passed |
| TC-098 | Reservation Lifecycle | `test_signed_document_upload_moves_reservation_to_document_review_with_verification_deadline` | Signed document upload starts staff document review deadline. | 1. Create held reservation.<br>2. Upload signed document.<br>3. Inspect status and deadline. | Signed approval document. | Status becomes pending document review with verification deadline. | Passed |
| TC-099 | Reservation Lifecycle | `test_submission_hold_sets_initial_document_upload_status_and_deadline` | Reservation submission creates initial document upload hold. | 1. Submit reservation.<br>2. Inspect reservation state.<br>3. Assert initial deadline. | Reservation submission details. | Status and document upload deadline are initialized. | Passed |
| TC-100 | Reservation Lifecycle | `test_document_rejection_records_reason_and_rejects_reservation` | Record document rejection reason and reject reservation. | 1. Create document-review reservation.<br>2. Reject with reason.<br>3. Inspect status and reason. | Document rejection reason. | Reservation is rejected and reason is stored. | Passed |
| TC-101 | Reservation Lifecycle | `test_payment_receipt_upload_records_verification_deadline_without_changing_payment_status` | Receipt upload records verification deadline without changing payment status. | 1. Create pending payment reservation.<br>2. Upload receipt.<br>3. Inspect status and deadline. | Payment receipt file. | Status remains pending payment and verification deadline is set. | Passed |
| TC-102 | Reservation Lifecycle | `test_payment_review_approves_or_rejects_reservation` | Payment review can approve or reject reservation. | 1. Create receipt-ready reservations.<br>2. Apply approve and reject decisions.<br>3. Assert resulting states. | Payment approval and rejection decisions. | Approval moves to approved; rejection moves to rejected. | Passed |
| TC-103 | Reservation Lifecycle | `test_cancellation_transitions_record_request_and_review_decisions` | Record cancellation requests and review decisions. | 1. Create approved reservation.<br>2. Request cancellation.<br>3. Review request and assert state. | Cancellation reason and staff decision. | Cancellation request and decision are recorded. | Passed |
| TC-104 | Reservation Lifecycle | `test_effective_status_treats_past_approved_reservation_as_completed` | Treat past approved reservation as completed in effective status. | 1. Create approved past reservation.<br>2. Read effective status.<br>3. Assert completed. | Past approved reservation end time. | Effective status is completed. | Passed |
| TC-105 | Reservation Lifecycle | `test_deadline_processing_expires_student_delay_and_marks_staff_overdue` | Lifecycle deadline processing handles student delay and staff overdue. | 1. Create overdue lifecycle records.<br>2. Process deadlines.<br>3. Assert status transitions. | Expired student and staff deadline timestamps. | Student delay expires and staff overdue is marked. | Passed |
| TC-106 | Reservation Submission | `test_reservation_submission_rejects_commit_time_conflict_after_available_time_selection` | Reject reservation submission if conflict appears after availability check. | 1. Validate available time.<br>2. Create conflicting reservation before submission.<br>3. Submit reservation. | Previously available time range with later conflict. | Submission is rejected for conflict. | Passed |
| TC-107 | Reservation Submission | `test_student_submits_reservation_details_and_views_held_reservation` | Allow student to submit details and view held reservation. | 1. Register/login student.<br>2. Submit reservation payload.<br>3. Request reservation detail. | Activity title, description, participants, organization, schedule. | Held reservation is created and visible to owner. | Passed |
| TC-108 | Reservation Submission | `test_student_reservation_submission_rejects_overlapping_blocking_reservation` | Reject submission overlapping an existing blocking reservation. | 1. Create blocking reservation.<br>2. Submit overlapping request.<br>3. Assert error. | Overlapping start/end timestamps. | Submission is rejected. | Passed |
| TC-109 | Reservation Submission | `test_student_reservation_submission_rejects_inactive_facility_and_organization_unit` | Reject submissions for inactive facility or organization unit. | 1. Create inactive facility and organization unit.<br>2. Submit reservation.<br>3. Assert errors. | Inactive facility ID and inactive organization unit ID. | Submission is rejected. | Passed |
| TC-110 | Reservation Submission | `test_student_reservation_submission_creates_unique_human_readable_codes` | Generate unique human-readable reservation codes. | 1. Submit multiple reservations.<br>2. Collect generated codes.<br>3. Assert uniqueness and format. | Multiple valid reservation submissions. | Each reservation gets a unique readable code. | Passed |
| TC-111 | Reviews | `test_student_creates_one_review_for_own_completed_reservation` | Allow one review for own completed reservation. | 1. Create completed reservation for student.<br>2. Submit review.<br>3. Try duplicate if applicable and read result. | Rating and comment for completed reservation. | Review is created once for owner. | Passed |
| TC-112 | Reviews | `test_student_soft_deletes_own_review_and_public_facility_excludes_it` | Let student soft-delete own review and hide it publicly. | 1. Create review.<br>2. Soft-delete as owner.<br>3. Request public facility reviews. | Student-owned review. | Deleted review is excluded from public facility output. | Passed |
| TC-113 | Reviews | `test_assigned_staff_views_facility_reviews_and_operational_statistics_read_only` | Allow assigned staff to view reviews and read-only operational stats. | 1. Create assigned staff and facility reviews.<br>2. Request staff review dashboard.<br>3. Assert data and read-only behavior. | Assigned facility, reviews, operational metrics. | Staff sees facility reviews/statistics without edit capability. | Passed |
| TC-114 | Reviews | `test_review_submission_requires_owner_completed_reservation_valid_rating_and_has_no_edit_route` | Enforce owner, completion, rating, and no-edit review rules. | 1. Try review with wrong owner, non-completed reservation, invalid rating.<br>2. Try edit route.<br>3. Assert failures. | Invalid owner/status/rating and edit request. | Invalid submissions and edit route are rejected. | Passed |
| TC-115 | Reviews | `test_super_admin_lists_all_reviews_globally_including_student_deleted_reviews` | Allow super admin to list all reviews including student-deleted ones. | 1. Create visible and student-deleted reviews.<br>2. Login as super admin.<br>3. Request global review list. | Mixed review statuses. | Global list includes all reviews. | Passed |
| TC-116 | Reviews | `test_super_admin_soft_deletes_review_with_reason_and_restores_without_editing_content` | Allow admin moderation delete/restore without content edits. | 1. Soft-delete review with reason as admin.<br>2. Restore review.<br>3. Assert content unchanged. | Admin deletion reason and review ID. | Review is deleted/restored while original content remains unchanged. | Passed |
| TC-117 | Reviews | `test_super_admin_views_immutable_audit_logs_with_filters_for_review_and_staff_assignment_actions` | Expose immutable audit logs with filters. | 1. Perform review and staff assignment actions.<br>2. Request audit logs with filters.<br>3. Assert filtered immutable entries. | Audit action filters. | Super admin receives matching audit logs. | Passed |
| TC-118 | Settings | `test_settings_module_loads_environment_and_normalizes_allowed_domains` | Load environment settings and normalize allowed domains. | 1. Set environment variables.<br>2. Load settings.<br>3. Assert normalized fields. | Allowed email domains and environment values. | Domains are normalized and settings load correctly. | Passed |
| TC-119 | Settings | `test_production_settings_require_explicit_safe_secret_and_database` | Require safe secret and database URL in production. | 1. Set production environment with unsafe/missing values.<br>2. Load settings.<br>3. Assert validation error. | Production environment, secret key, database URL. | Unsafe production config is rejected. | Passed |
| TC-120 | Settings | `test_application_settings_do_not_construct_booking_settings` | Keep application settings separate from booking settings construction. | 1. Load application settings.<br>2. Inspect constructed settings.<br>3. Assert booking settings are not created there. | Application settings configuration. | Application settings do not construct booking settings. | Passed |
| TC-121 | Staff Facility Management | `test_super_admin_assigns_staff_and_staff_manages_only_assigned_facility` | Super admin assigns staff and staff manages only assigned facility. | 1. Login as super admin and assign staff.<br>2. Login as staff.<br>3. Attempt management on assigned and unassigned facilities. | Staff ID, assigned facility ID, unassigned facility ID. | Staff can manage assigned facility only. | Passed |
| TC-122 | Staff Facility Management | `test_assigned_staff_manages_facility_images_open_hours_and_blackouts` | Assigned staff can manage facility operational data. | 1. Login as assigned staff.<br>2. Update images, open hours, and blackouts.<br>3. Read facility data. | Image payload, open-hour payload, blackout payload. | Facility images, open hours, and blackouts are updated. | Passed |
| TC-123 | Staff Reservation Review Access | `test_staff_reservation_review_access_returns_assigned_reservation` | Return reservation for assigned staff reviewer. | 1. Create assigned facility reservation.<br>2. Request review access as assigned staff.<br>3. Assert reservation returned. | Assigned staff ID and reservation ID. | Assigned reservation is returned. | Passed |
| TC-124 | Staff Reservation Review Access | `test_staff_reservation_review_access_denies_existing_unassigned_reservation` | Deny review access to unassigned staff. | 1. Create reservation for another staff assignment.<br>2. Request review access.<br>3. Assert denial. | Unassigned staff ID and existing reservation ID. | Access is denied. | Passed |
| TC-125 | Staff Reservation Review Access | `test_staff_reservation_review_access_reports_missing_reservation` | Report missing reservation during staff review access. | 1. Request nonexistent reservation.<br>2. Evaluate access.<br>3. Assert missing result. | Missing reservation ID. | Missing reservation is reported distinctly. | Passed |
| TC-126 | System Status | `test_demo_smoke_check_reports_backend_and_database_health` | Report backend and database health in demo smoke check. | 1. Start app with working database.<br>2. Call smoke-check endpoint.<br>3. Assert health fields. | In-memory database. | Backend and database health are reported as healthy. | Passed |
| TC-127 | System Status | `test_demo_smoke_check_fails_when_database_is_unavailable` | Fail smoke check when database is unavailable. | 1. Configure unavailable database dependency.<br>2. Call smoke-check endpoint.<br>3. Assert unhealthy result. | Broken database connection. | Smoke check reports database failure. | Passed |
| TC-128 | System Status | `test_super_admin_views_read_only_system_status` | Allow super admin to view read-only system status. | 1. Login as super admin.<br>2. Request system status.<br>3. Assert read-only status payload. | Super admin token. | System status is returned. | Passed |
| TC-129 | System Status | `test_non_admin_users_cannot_view_system_status` | Prevent non-admin users from viewing system status. | 1. Login as non-admin.<br>2. Request system status.<br>3. Assert authorization failure. | Student or staff token. | Access is denied. | Passed |
| TC-130 | User Repository | `test_sqlalchemy_user_repository_stores_and_finds_user_accounts` | Verify SQLAlchemy user repository stores and retrieves accounts. | 1. Store a user account through repository.<br>2. Find user by lookup methods.<br>3. Assert retrieved fields. | User account record. | Repository stores and finds user account correctly. | Passed |
